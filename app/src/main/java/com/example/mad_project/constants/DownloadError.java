package com.example.mad_project.constants;

public enum DownloadError {
    NETWORK_UNAVAILABLE,
    CONNECTION_TIMEOUT,
    CONNECTION_LOST,
    FILE_ERROR,
    INVALID_URL,
    INSUFFICIENT_STORAGE,
    UNEXPECTED_ERROR
}